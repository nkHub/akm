{
  "name": "error_handler",
  "version": "0.1.0",
  "has_menu": false,
  "category": "handler",
  "description": "上游错误处理：429 切换 key、402/401/403 禁用 key、5xx 指数退避重试",
  "builtin": false,
  "default_enabled": true,
  "required": false,
  "hooks": {
    "on_upstream_error": true
  },
  "settings": [
    {
      "key": "max_retries_per_key",
      "label": "单 key 最大重试次数",
      "type": "number",
      "default": 3,
      "description": "同个 key 遇到 5xx 或连接错误时的指数退避重试上限"
    },
    {
      "key": "max_key_tries",
      "label": "最大 key 切换次数",
      "type": "number",
      "default": 5,
      "description": "所有 key 全部失败前最多尝试几个 key"
    },
    {
      "key": "auto_disable",
      "label": "自动禁用/限流 key",
      "type": "boolean",
      "default": true,
      "description": "遇到 429 限流或 401/402/403 认证失败时自动把 key 标记为限流/禁用；关闭后仅切换 key，不改变 key 状态"
    }
  ]
}
