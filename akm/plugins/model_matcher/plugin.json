{
  "name": "model_matcher",
  "version": "0.1.0",
  "has_menu": false,
  "category": "matcher",
  "description": "模型匹配路由：根据请求模型名匹配可用的 API key，支持模型别名映射和通配符兜底",
  "builtin": true,
  "required": true,
  "hooks": {
    "on_request": true,
    "on_key_selected": true
  },
  "settings": [
    {
      "key": "aliases",
      "label": "模型别名映射",
      "type": "text",
      "default": "",
      "description": "模型名映射规则，格式: old1=new1,old2=new2，如 gpt-4=gpt-4-turbo"
    },
    {
      "key": "force_tool_choice_required_for_gpt",
      "label": "GPT工具强制调用",
      "type": "boolean",
      "default": true,
      "description": "对 gpt-*/codex 且带 tools 的请求，在未显式提供 tool_choice 时自动设置为 required"
    }
  ]
}
